"use client"; import {useState} from "react"; import {Button} from "@/components/ui/button"; import {Input} from "@/components/ui/input";
export function RecoveryRequestForm(){const[m,setM]=useState(""); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const f=new FormData(e.currentTarget); await fetch("/api/auth/recovery",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:String(f.get("email")??"")})}); setM("If that account exists, a recovery email is on the way.");} return <form onSubmit={submit} className="flex flex-col gap-5"><h2 className="font-display text-[28px] text-brand-navy">Reset password</h2><Input label="Email" name="email" type="email" required/><Button variant="gold" type="submit">Send recovery email</Button>{m?<p className="text-sm text-neutral-700">{m}</p>:null}</form>;}
