"use client"; import {useState} from "react"; import {Button} from "@/components/ui/button"; import {Input} from "@/components/ui/input"; import { createBrowserClient } from "@supabase/ssr";
export function ResetPasswordForm(){const[m,setM]=useState(""); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const f=new FormData(e.currentTarget); const supabase=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!); const {error}=await supabase.auth.updateUser({password:String(f.get("password")??"")}); setM(error?error.message:"Password updated. You can now sign in.");} return <form onSubmit={submit} className="flex flex-col gap-5"><h2 className="font-display text-[28px] text-brand-navy">Choose a new password</h2><Input label="New password" name="password" type="password" required minLength={8}/><Button variant="gold" type="submit">Update password</Button>{m?<p className="text-sm text-neutral-700">{m}</p>:null}</form>;}
